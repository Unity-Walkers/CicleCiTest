machine:
  java:
    version: openjdk7

dependeincies:
  override:
    - sudo pip install swscli

test:
  override:
    - javac ./jp/unitywalkers/Main.java
    - java jp.unitywalkers.Main

deployment:
  aerith:
    branch: master
    commands:
      - chmod 777 ./notice.sh
      - ./notice.sh
      - chmod 777 ./publish.sh
      - ./publish.sh